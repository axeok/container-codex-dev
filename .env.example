MODEL_PROVIDER_ROUTE=
MODEL_PROVIDER_API_TOKEN=

# Optional public key used to initialize /root/.ssh/authorized_keys.
SSH_AUTHORIZED_KEY=

# Generate with: openssl rand -hex 32
CODEX_GATEWAY_CONFIG_SECRET=replace-with-a-stable-random-secret
CODEX_GATEWAY_ADMIN_USERNAME=admin
CODEX_GATEWAY_ADMIN_PASSWORD=replace-with-a-password-of-at-least-8-characters
BROWSER_PREVIEW_DOMAIN=localhost
